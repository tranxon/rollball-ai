//! Settings/config commands
//
// Config and log management are now handled directly by the frontend
// via fetch() to the Gateway HTTP API (getGatewayUrl()). This ensures
// correct behavior in both local and remote Gateway scenarios.
